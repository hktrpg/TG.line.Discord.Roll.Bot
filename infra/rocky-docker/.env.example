#
# Docker Compose substitution file (password for MongoDB root user).
#
# Create (or rename) this file to `infra/rocky-docker/.env` OR export the
# variable `MONGO_INITDB_ROOT_PASSWORD` in your shell before running compose.
#

MONGO_INITDB_ROOT_PASSWORD=REPLACE_ME_WITH_A_LONG_STRONG_PASSWORD

